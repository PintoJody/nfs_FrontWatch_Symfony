<div class="col mb-5">
	<div
		class="card h-100">
		<!-- Product image-->
		<img
		class="card-img-top" src="{{watch.picture}}" alt="{{watch.name}}"/>
		<!-- Product details-->
		<div class="card-body p-4">
			<div
				class="text-center">
				<!-- Product name-->
				<h5 class="fw-bolder">{{watch.name}}</h5>
				<!-- Product price-->
				{{watch.price|number_format(2, '.')}}€
			</div>
		</div>
		<!-- Product actions-->
		<div class="card-footer p-4 pt-0 border-top-0 bg-transparent">
			<div class="text-center">
				<a class="btn btn-outline-dark mt-auto" href="{{ path('app_watch_show',{'id':watch.id}) }}">Voir plus</a>
			</div>
		</div>
	</div>
</div>
